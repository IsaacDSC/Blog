{{#each erros}}
<div class="alert alert-danger">{{texto}}</div>
{{else}}
{{/each}}
<h3>Nova categoria:</h3>
<div class="card">
    <div class="card-body">
        <form action="/admin/new" method="post">
            <label for="">Nome: </label>
            <input type="text" name="nome" id="nome" placeholder="Nome da Categoria" class="form-control">
            <label for="">Slug: </label>
            <input type="text" id="slug" name="slug" placeholder="Slug da categotria" class="form-control">
            <br>
            <button class="bnt btn-success">Criar categoria</button>
        </form>
    </div>
</div>